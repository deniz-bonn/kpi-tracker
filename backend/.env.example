# DB_DIALECT=sqlite (default) | postgres
DB_DIALECT=sqlite
DB_PATH=./data/kpi.db

# PostgreSQL (nur wenn DB_DIALECT=postgres)
PGHOST=localhost
PGPORT=5432
PGDATABASE=kpi_tracker
PGUSER=kpi_user
PGPASSWORD=changeme

PORT=3001
